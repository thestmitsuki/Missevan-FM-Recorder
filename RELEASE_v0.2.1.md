# Release Notes — v0.2.1

> Missevan FM Recorder（猫耳 FM 录制器）· 2026-08-22
>
> 上一个发布：v0.2.0

## 概述

v0.2.1 是维护修复版本，聚焦**录制稳定性**与**发布流程**：修复「主播正常下播被误判为崩溃」这一关键问题（完整录音曾被改名 `.part` 并遭清理删除），同时重构自动更新的下载匹配逻辑、加固 Arch 打包流程，并清理 Linux 窗口控制实现。

## 修复

- **正常下播不再误判崩溃（B1）**：原实现忽略 ffmpeg 退出码，主播下播（ffmpeg 读到流 EOF、退出码 0）被误判为 REC_CRASH，导致完整录音被改名 `.part`、下次启动被清理删除、崩溃熔断计数累积。现按退出码判定：
  - 退出码 0 且非用户取消 → 正常结束（主播下播 / 流断开）
  - 用户主动停止时子进程退出 → 正常停止，不再计入崩溃
- **下播后录制任务自动收尾**：正常结束后 monitor 不再空转，主动结束本轮录制；主播仍在播则由检测循环自动恢复，保留「流断自动重连」能力
- **ffmpeg 输出改为 `-n` 不覆盖模式**：防止意外覆盖已有录音文件
- **分段录制残留兼容**：输出目录存在 `{前缀}_{NNN}.{ext}` 残留段文件时自动前缀去重（`_2`/`_3`…），避免 ffmpeg 因首段文件已存在而拒绝启动

## 改进

- **自动更新按平台关键词匹配**：下载资产改为按 `win` / `appimage` / `arch` 关键词匹配（原按后缀匹配），避免资产后缀变化（如 `.exe` 改 `.msi`）导致匹配失败；无匹配资产时回退发布页，**绝不下载错误平台的包**
- **FFmpeg zip SHA256 校验框架**：校验逻辑已就绪（当前期望值为占位 `None`，跳过校验）；确定官方构建哈希来源后填入即自动启用强校验，下载损坏/被篡改的 zip 在解压前拦截
- **Arch 打包加固**：PKGBUILD 拆分并新增 desktop 文件；发布流水线校验 PKG 布局（所有条目必须位于 `/usr` 下，防 usrmerge 回归）
- **Linux 窗口控制简化**：移除自绘窗口控制，由窗口管理器接管（配合 Arch 包无系统标题栏）

## 工程化

- **CI**：`cargo check` 前创建 `dist/` 占位目录（Tauri `generate_context!` 编译期要求 frontendDist 存在）；声明最小权限（`contents: read` / `write`）
- **测试**：修复 Linux CI 上的平台相关断言（cargo test 跨平台通过）
- **依赖**：npm 依赖更新（含 lockfile 刷新）

## 兼容性说明

- 配置与数据结构无变更，v0.2.0 可直接升级
- 最低系统要求不变：Windows 10/11、Linux（见 INSTALL.md）

## 下载

安装包由 CI 构建并发布至 GitHub Releases（tag `v0.2.1`）：

- Windows：`*-setup.exe`（NSIS 安装包）/ 单文件便携版 EXE
- Linux：`*.AppImage`（AppImage 版）
- Arch Linux：`missevan-recorder-*-x86_64.pkg.tar.zst`（PKG 版）
